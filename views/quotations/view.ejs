<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Enhanced Quotation View Design - Based on Bill View */
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .quotation-container {
            max-width: 1200px;
            margin: 30px auto;
            background: white;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
            border-radius: 16px;
            overflow: visible;
            position: relative;
        }
        
        .quotation-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(90deg, #8b5cf6, #a855f7, #8b5cf6);
        }
        
        .quotation-header {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 40px;
            position: relative;
        }
        
        .company-info {
            text-align: center;
            margin-bottom: 30px;
            position: relative;
        }
        
        .company-name {
            font-size: 42px;
            font-weight: 700;
            color: #8b5cf6;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(139, 92, 246, 0.1);
            letter-spacing: 1px;
        }
        
        .company-details {
            font-size: 16px;
            color: #6c757d;
            line-height: 1.6;
            font-weight: 500;
        }
        
        .quotation-title {
            text-align: center;
            font-size: 32px;
            font-weight: 700;
            color: #495057;
            margin: 40px 0;
            text-transform: uppercase;
            letter-spacing: 3px;
            position: relative;
        }
        
        .quotation-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: linear-gradient(90deg, #8b5cf6, #a855f7);
            border-radius: 2px;
        }
        
        /* GST vs Non-GST Styling */
        .bill-type-indicator {
            text-align: center;
            margin: 20px 0;
        }
        
        .bill-type-badge {
            display: inline-block;
            padding: 10px 20px;
            border-radius: 25px;
            font-weight: 700;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .gst-badge {
            background: linear-gradient(135deg, #d4edda, #c3e6cb);
            color: #155724;
            border: 2px solid #28a745;
        }
        
        .non-gst-badge {
            background: linear-gradient(135deg, #fff3cd, #ffeaa7);
            color: #856404;
            border: 2px solid #ffc107;
        }
        
        .quotation-info {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            padding: 0 40px 40px;
        }
        
        .bill-to, .bill-details {
            background: white;
            border-radius: 16px;
            padding: 30px;
            box-shadow: 0 8px 25px rgba(139, 92, 246, 0.12);
            border: 1px solid #f0f8ff;
            position: relative;
            overflow: hidden;
        }
        
        .bill-to::before, .bill-details::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, #8b5cf6, #a855f7);
            border-radius: 12px 12px 0 0;
        }
        
        .section-title {
            font-weight: 700;
            color: #8b5cf6;
            margin-bottom: 20px;
            border-bottom: 2px solid #8b5cf6;
            padding-bottom: 10px;
            font-size: 18px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .section-title::before {
            content: '';
            width: 8px;
            height: 8px;
            background: #8b5cf6;
            border-radius: 50%;
        }
        
        .info-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #f1f3f4;
            min-height: 20px;
        }
        
        .info-row:last-child {
            border-bottom: none;
        }
        
        .info-label {
            font-weight: 600;
            color: #6c757d;
            font-size: 14px;
            white-space: nowrap;
        }
        
        .info-value {
            font-weight: 700;
            color: #495057;
            font-size: 14px;
            text-align: right;
        }
        
        .items-section, .services-section {
            padding: 0 20px 40px;
        }
        
        .items-table-wrapper, .services-table-wrapper {
            overflow-x: auto;
            margin-bottom: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        }
        
        .items-header, .services-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 25px;
            font-size: 24px;
            font-weight: 700;
            color: #495057;
        }
        
        .items-header::before {
            content: '📦';
            font-size: 28px;
        }
        
        .services-header::before {
            content: '⚙️';
            font-size: 28px;
        }
        
        .items-table, .services-table {
            width: 100%;
            border-collapse: collapse;
            border-radius: 12px;
            overflow: hidden;
            min-width: 600px;
            table-layout: auto;
        }
        
        .items-table th,
        .items-table td,
        .services-table th,
        .services-table td {
            border: 1px solid #e9ecef;
            padding: 12px 8px;
            vertical-align: middle;
            white-space: nowrap;
        }
        
        .items-table .item-name,
        .services-table .service-name {
            white-space: normal;
            word-wrap: break-word;
            min-width: 200px;
            max-width: 350px;
            font-weight: 700;
            color: #495057;
        }
        
        .items-table th,
        .services-table th {
            background: linear-gradient(135deg, #8b5cf6 0%, #a855f7 100%);
            color: white;
            padding: 18px 15px;
            text-align: center;
            font-weight: 700;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            vertical-align: middle;
            border: 1px solid #7c3aed;
        }
        
        .services-table th {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            border: 1px solid #059669;
        }
        
        .items-table td,
        .services-table td {
            padding: 16px 15px;
            border-bottom: 1px solid #f1f3f4;
            border-right: 1px solid #f1f3f4;
            font-size: 14px;
            background: white;
            vertical-align: middle;
        }
        
        .items-table .text-right,
        .services-table .text-right {
            text-align: right;
        }
        
        .items-table .text-center,
        .services-table .text-center {
            text-align: center;
        }
        
        .items-table tbody tr:nth-child(even) {
            background: #faf5ff;
        }
        
        .items-table tbody tr:hover {
            background: #f3e8ff;
            transition: background-color 0.3s ease;
        }
        
        .services-table tbody tr:nth-child(even) {
            background: #f0fff4;
        }
        
        .services-table tbody tr:hover {
            background: #d4edda;
            transition: background-color 0.3s ease;
        }
        
        /* Totals Section */
        .totals-section {
            margin: 40px 20px;
        }
        
        .totals-wrapper {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
        }
        
        .payment-card, .totals-card {
            background: white;
            border-radius: 16px;
            padding: 30px;
            box-shadow: 0 8px 25px rgba(139, 92, 246, 0.15);
            border: 1px solid #f3e8ff;
        }
        
        .card-header {
            font-size: 18px;
            font-weight: 700;
            color: #8b5cf6;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #8b5cf6;
        }
        
        .payment-row, .total-row-new {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #f1f3f4;
        }
        
        .payment-row:last-child, .total-row-new:last-child {
            border-bottom: none;
        }
        
        .row-label {
            font-weight: 600;
            color: #6c757d;
            font-size: 14px;
        }
        
        .row-value {
            font-weight: 700;
            color: #495057;
            font-size: 14px;
        }
        
        .final-amount {
            background: linear-gradient(135deg, #8b5cf6, #a855f7);
            border-radius: 12px;
            padding: 20px;
            margin-top: 20px;
            color: white;
            text-align: center;
        }
        
        .amount-label {
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 8px;
            opacity: 0.9;
        }
        
        .amount-value {
            font-size: 28px;
            font-weight: 700;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }
        
        .status-badge {
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .status-valid {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        
        .status-expired {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        
        .status-converted {
            background: #d1ecf1;
            color: #0c5460;
            border: 1px solid #bee5eb;
        }
        
        /* Footer Information Styles */
        .footer-info {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            padding: 30px 40px;
            background: #f8f9fa;
            border-top: 1px solid #e9ecef;
        }
        
        .terms-section h4,
        .validity-section h4 {
            color: #8b5cf6;
            font-size: 16px;
            font-weight: 700;
            margin-bottom: 15px;
            border-bottom: 2px solid #8b5cf6;
            padding-bottom: 5px;
        }
        
        .terms-section ul {
            list-style: none;
            padding-left: 0;
            margin: 0;
        }
        
        .terms-section ul li {
            padding: 8px 0;
            border-bottom: 1px solid #e9ecef;
            color: #495057;
            font-size: 14px;
            position: relative;
            padding-left: 20px;
        }
        
        .terms-section ul li::before {
            content: '✓';
            position: absolute;
            left: 0;
            color: #8b5cf6;
            font-weight: bold;
        }
        
        .terms-section ul li:last-child {
            border-bottom: none;
        }
        
        .validity-section p {
            margin: 8px 0;
            color: #495057;
            font-size: 14px;
        }
        
        .validity-section strong {
            color: #8b5cf6;
            font-weight: 600;
        }
        
        /* Enhanced Button Styles */
        .actions {
            padding: 30px 20px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-top: 1px solid #e9ecef;
        }
        
        .action-buttons {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 15px;
            flex-wrap: wrap;
        }
        
        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 14px 24px;
            border: none;
            border-radius: 12px;
            font-size: 14px;
            font-weight: 600;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            position: relative;
            overflow: hidden;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }
        
        .btn:hover::before {
            left: 100%;
        }
        
        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }
        
        .btn:active {
            transform: translateY(-1px);
        }
        
        .btn-secondary {
            background: linear-gradient(135deg, #6c757d, #495057);
            color: white;
        }
        
        .btn-secondary:hover {
            background: linear-gradient(135deg, #5a6268, #343a40);
            box-shadow: 0 8px 25px rgba(108, 117, 125, 0.4);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #8b5cf6, #7c3aed);
            color: white;
        }
        
        .btn-primary:hover {
            background: linear-gradient(135deg, #7c3aed, #6d28d9);
            box-shadow: 0 8px 25px rgba(139, 92, 246, 0.4);
        }
        
        .btn-success {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
        }
        
        .btn-success:hover {
            background: linear-gradient(135deg, #20c997, #17a2b8);
            box-shadow: 0 8px 25px rgba(40, 167, 69, 0.4);
        }
        
        .btn-convert {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            color: white;
        }
        
        .btn-convert:hover {
            background: linear-gradient(135deg, #2563eb, #1d4ed8);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4);
        }
        
        .btn-print-compact {
            background: linear-gradient(135deg, #10b981, #059669);
            color: white;
        }
        
        .btn-print-compact:hover {
            background: linear-gradient(135deg, #059669, #047857);
            box-shadow: 0 8px 25px rgba(16, 185, 129, 0.4);
        }
        
        .btn .icon {
            font-size: 16px;
            min-width: 16px;
        }
        
        .btn span {
            font-weight: 600;
        }
        
        
        /* Comprehensive Print Styles */
        @media print {
            /* Reset and basic setup */
            * {
                -webkit-print-color-adjust: exact !important;
                color-adjust: exact !important;
                print-color-adjust: exact !important;
            }
            
            body {
                background: white !important;
                margin: 0 !important;
                padding: 0 !important;
                font-size: 11px !important;
                line-height: 1.3 !important;
                color: #000 !important;
            }
            
            /* Hide unnecessary elements */
            .actions, .main-header, nav, .action-buttons, 
            .status-badge, .hover-effects, .btn,
            .quotation-container::before {
                display: none !important;
            }
            
            /* Container adjustments */
            .quotation-container {
                box-shadow: none !important;
                margin: 0 !important;
                padding: 0 !important;
                border-radius: 0 !important;
                max-width: 100% !important;
                background: white !important;
            }
            
            /* Header optimization */
            .quotation-header {
                background: white !important;
                padding: 20px 15px 15px 15px !important;
                border-bottom: 2px solid #8b5cf6 !important;
                page-break-inside: avoid;
            }
            
            .company-info {
                margin-bottom: 15px !important;
                text-align: center;
            }
            
            .company-name {
                font-size: 24px !important;
                font-weight: bold !important;
                color: #8b5cf6 !important;
                margin-bottom: 5px !important;
            }
            
            .company-address {
                font-size: 9px !important;
                line-height: 1.2 !important;
                margin-bottom: 10px !important;
            }
            
            .quotation-title {
                font-size: 18px !important;
                font-weight: bold !important;
                color: #8b5cf6 !important;
                text-align: center !important;
                margin: 10px 0 !important;
                text-transform: uppercase !important;
                letter-spacing: 1px !important;
            }
            
            /* Quotation info layout */
            .quotation-info {
                display: grid !important;
                grid-template-columns: 1fr 1fr !important;
                gap: 20px !important;
                padding: 15px !important;
                background: #f8f9fa !important;
                border: 1px solid #e9ecef !important;
                margin-bottom: 15px !important;
                page-break-inside: avoid;
            }
            
            .bill-to, .bill-details {
                font-size: 10px !important;
            }
            
            .section-title {
                font-weight: bold !important;
                color: #8b5cf6 !important;
                font-size: 11px !important;
                margin-bottom: 8px !important;
                border-bottom: 1px solid #8b5cf6 !important;
                padding-bottom: 2px !important;
            }
            
            /* Tables optimization */
            .items-table, .services-table {
                width: 100% !important;
                border-collapse: collapse !important;
                margin-bottom: 15px !important;
                font-size: 9px !important;
                page-break-inside: auto;
            }
            
            .items-table th, .services-table th,
            .items-table td, .services-table td {
                border: 1px solid #666 !important;
                padding: 4px 3px !important;
                text-align: left !important;
                vertical-align: top !important;
            }
            
            .items-table th, .services-table th {
                background: #8b5cf6 !important;
                color: white !important;
                font-weight: bold !important;
                font-size: 9px !important;
            }
            
            .items-table .amount-cell, .services-table .amount-cell,
            .items-table .rate-cell, .services-table .rate-cell {
                text-align: right !important;
            }
            
            /* Section headers */
            .items-header, .services-header {
                background: linear-gradient(135deg, #8b5cf6, #7c3aed) !important;
                color: white !important;
                padding: 8px 15px !important;
                font-weight: bold !important;
                font-size: 11px !important;
                margin: 15px 0 5px 0 !important;
                border-radius: 0 !important;
            }
            
            /* Totals section */
            .quotation-content {
                padding: 0 15px !important;
            }
            
            .totals-wrapper {
                display: grid !important;
                grid-template-columns: 1fr 300px !important;
                gap: 20px !important;
                margin-top: 15px !important;
                page-break-inside: avoid;
            }
            
            .payment-card, .totals-card {
                border: 1px solid #ddd !important;
                background: white !important;
                padding: 10px !important;
                font-size: 10px !important;
            }
            
            .card-header {
                background: #8b5cf6 !important;
                color: white !important;
                font-weight: bold !important;
                padding: 6px 10px !important;
                margin: -10px -10px 10px -10px !important;
                font-size: 10px !important;
            }
            
            .payment-row, .total-row {
                display: flex !important;
                justify-content: space-between !important;
                padding: 3px 0 !important;
                border-bottom: 1px dotted #ccc !important;
                font-size: 9px !important;
            }
            
            .total-row.final-total {
                border-top: 2px solid #8b5cf6 !important;
                font-weight: bold !important;
                font-size: 11px !important;
                background: #f8f9fa !important;
                padding: 6px 0 !important;
                margin-top: 5px !important;
            }
            
            /* Terms and conditions */
            .terms-section, .validity-section {
                margin-top: 15px !important;
                padding: 10px !important;
                border: 1px solid #ddd !important;
                background: #f8f9fa !important;
                font-size: 8px !important;
                page-break-inside: avoid;
            }
            
            .terms-section h4, .validity-section h4 {
                color: #8b5cf6 !important;
                font-size: 10px !important;
                margin: 0 0 8px 0 !important;
                border-bottom: 1px solid #8b5cf6 !important;
                padding-bottom: 2px !important;
            }
            
            .terms-section ul {
                margin: 0 !important;
                padding-left: 15px !important;
            }
            
            .terms-section li {
                margin-bottom: 3px !important;
                line-height: 1.2 !important;
            }
            
            /* Footer */
            .footer-info {
                margin-top: 20px !important;
                padding: 10px !important;
                border-top: 2px solid #8b5cf6 !important;
                text-align: center !important;
                font-size: 8px !important;
                background: #f8f9fa !important;
                page-break-inside: avoid;
            }
            
            /* Page breaks */
            .quotation-header {
                page-break-after: avoid;
            }
            
            .quotation-info {
                page-break-after: avoid;
            }
            
            .items-header {
                page-break-after: avoid;
            }
            
            .services-header {
                page-break-after: avoid;
            }
            
            .totals-wrapper {
                page-break-before: avoid;
                page-break-inside: avoid;
            }
            
            /* Utility classes for print */
            .no-print {
                display: none !important;
            }
            
            .print-only {
                display: block !important;
            }
            
            /* Compact spacing */
            .quotation-content > * {
                margin-bottom: 10px !important;
            }
            
            /* Ensure proper colors */
            .purple-text {
                color: #8b5cf6 !important;
            }
            
            /* Print-specific adjustments */
            @page {
                margin: 0.75in 0.5in;
                size: A4;
            }
            
            /* Force page break before totals if needed */
            .force-page-break {
                page-break-before: always;
            }
        }
        
        @media (max-width: 768px) {
            .quotation-container {
                margin: 10px;
                border-radius: 8px;
            }
            
            .quotation-header {
                padding: 20px;
            }
            
            .company-name {
                font-size: 28px;
            }
            
            .quotation-title {
                font-size: 24px;
            }
            
            .quotation-info {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            
            .totals-wrapper {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            
            .footer-info {
                grid-template-columns: 1fr;
                gap: 20px;
                padding: 20px;
            }
            
            .items-table-wrapper,
            .services-table-wrapper {
                overflow-x: auto;
            }
            
            .action-buttons {
                flex-direction: column;
                gap: 10px;
            }
        }
    </style>
    <%- include('../partials/styles') %>
</head>
<body>
    <%- include('../partials/header') %>
<nav class="breadcrumb">
        <div class="breadcrumb-content">
            <a href="/dashboard">🏠 Dashboard</a>
            <span class="separator">></span>
            <a href="/quotations">💜 Quotations</a>
            <span class="separator">></span>
            <span class="current">💜 Quotation #<%= quotation.quotationNumber %></span>
        </div>
    </nav>
    <main class="main-content">
        <!-- Alert Messages -->
    <% if (typeof query !== 'undefined' && query.success) { %>
        <div class="alert alert-success" style="margin: 20px auto; max-width: 800px;">
            <%= query.success %>
        </div>
    <% } %>

    <!-- Quotation Container -->
    <div class="quotation-container">
        <!-- Quotation Header -->
        <div class="quotation-header">
            <div class="company-info">
                <div class="company-name"><%= company.name %></div>
                <div class="company-details">
                    <%= company.address.line1 %>, <%= company.address.line2 %><br>
                    <%= company.address.city %>, <%= company.address.state %> - <%= company.address.pincode %><br>
                    Cell: <%= company.contact.phone1 %>, <%= company.contact.phone2 %><br>
                    <% if (company.gst.gstin) { %>GSTIN: <%= company.gst.gstin %><% } %>
                </div>
            </div>
            
            <div class="quotation-title">
                <% if (quotation.gstEnabled && (quotation.documentType === 'GST' || quotation.documentType === 'gst')) { %>
                    Tax Quotation
                <% } else { %>
                    Quotation
                <% } %>
            </div>
            
            <div class="bill-type-indicator">
                <% if (quotation.gstEnabled && (quotation.documentType === 'GST' || quotation.documentType === 'gst')) { %>
                    <span class="bill-type-badge gst-badge">🏷️ GST Quotation</span>
                <% } else { %>
                    <span class="bill-type-badge non-gst-badge">📄 Non-GST Quotation</span>
                <% } %>
            </div>
        </div>

        <!-- Quotation Info -->
        <div class="quotation-info">
            <div class="bill-to">
                <div class="section-title">Quote To:</div>
                <strong><%= quotation.customerInfo?.name || 'Unknown Customer' %></strong><br>
                Phone: <%= quotation.customerInfo?.phone || 'N/A' %><br>
                <% if (quotation.customerInfo?.gstin && quotation.customerInfo.gstin.trim() !== '') { %>
                    GSTIN: <%= quotation.customerInfo.gstin %>
                <% } else { %>
                    <em>Unregistered Customer</em>
                <% } %>
            </div>
            
            <div class="bill-details">
                <div class="section-title">Quotation Details:</div>
                <% 
                const currentDate = new Date();
                const quotationDate = new Date(quotation.quotationDate);
                const validityDate = new Date(quotationDate.getTime() + quotation.validityDays*24*60*60*1000);
                let status = 'Valid';
                if (quotation.status === 'converted') {
                    status = 'Converted';
                } else if (currentDate > validityDate) {
                    status = 'Expired';
                }
                %>
                <strong>Quote No:</strong> #<%= quotation.quotationNumber %><br>
                <strong>Date:</strong> <%= new Date(quotation.quotationDate).toLocaleDateString('en-IN') %><br>
                <strong>Valid Till:</strong> <%= validityDate.toLocaleDateString('en-IN') %><br>
                <strong>Status:</strong> 
                <span class="status-badge status-<%= status.toLowerCase() %>"><%= status.toUpperCase() %></span><br>
                <strong>Created By:</strong> <%= user.name %>
            </div>
        </div>

        <!-- Items Section -->
        <div class="items-section">
            <div class="items-header">
                <h3>Items</h3>
                <div class="items-count" style="background: linear-gradient(135deg, #8b5cf6, #a855f7); color: white; padding: 8px 16px; border-radius: 20px; font-weight: 600; font-size: 14px; box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);"><%= quotation.items.length %> item(s)</div>
            </div>
            
            <div class="items-table-wrapper">
                <table class="items-table">
                    <thead>
                        <tr>
                            <th style="width: 60px; text-align: center;">S.No</th>
                            <th style="width: auto; text-align: left; min-width: 200px;">Description</th>
                            <th style="width: 100px; text-align: center;">HSN</th>
                            <th style="width: 80px; text-align: center;">Qty</th>
                            <th style="width: 120px; text-align: right;">Rate</th>
                            <% if (quotation.gstEnabled && (quotation.documentType === 'GST' || quotation.documentType === 'gst')) { %>
                            <th style="width: 120px; text-align: right;">Amount</th>
                            <th style="width: 80px; text-align: center;">GST</th>
                            <% } else { %>
                            <th style="width: 150px; text-align: right;">Amount</th>
                            <% } %>
                        </tr>
                    </thead>
                    <tbody>
                        <% let itemCounter = 1; %>
                        <% quotation.items.forEach(item => { %>
                            <tr class="item-row">
                                <td class="text-center"><%= itemCounter %></td>
                                <td class="item-name">
                                    <%= item.itemName %>
                                    <% if (item.bundleInfo) { %>
                                        <br><small style="color: #6c757d; font-style: italic;"><%= item.bundleInfo %></small>
                                    <% } %>
                                </td>
                                <td class="text-center"><%= item.hsnCode || '-' %></td>
                                <td class="text-center"><%= item.quantity %></td>
                                <td class="text-right">₹<%= item.rate.toFixed(2) %></td>
                                <% if (quotation.gstEnabled && (quotation.documentType === 'GST' || quotation.documentType === 'gst')) { %>
                                <td class="text-right amount-cell">₹<%= item.amount.toFixed(2) %></td>
                                <td class="text-center"><%= item.gstRate || 0 %>%</td>
                                <% } else { %>
                                <td class="text-right amount-cell">₹<%= item.amount.toFixed(2) %></td>
                                <% } %>
                            </tr>
                            <% itemCounter++; %>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Services Section -->
        <% if (quotation.extraCharges && quotation.extraCharges.length > 0) { %>
        <div class="services-section">
            <div class="services-header">
                <h3>Additional Services</h3>
                <div class="services-count" style="background: linear-gradient(135deg, #10b981, #059669); color: white; padding: 8px 16px; border-radius: 20px; font-weight: 600; font-size: 14px; box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);"><%= quotation.extraCharges.length %> service(s)</div>
            </div>
            
            <div class="services-table-wrapper">
                <table class="services-table">
                    <thead>
                        <tr>
                            <th style="width: 60px; text-align: center;">S.No</th>
                            <th style="width: auto; text-align: left; min-width: 250px;">Service Description</th>
                            <th style="width: 100px; text-align: center;">SAC Code</th>
                            <% if (quotation.gstEnabled && (quotation.documentType === 'GST' || quotation.documentType === 'gst')) { %>
                            <th style="width: 120px; text-align: right;">Amount</th>
                            <th style="width: 100px; text-align: center;">GST Rate</th>
                            <% } else { %>
                            <th style="width: 150px; text-align: right;">Amount</th>
                            <% } %>
                        </tr>
                    </thead>
                    <tbody>
                        <% let serviceCounter = 1; %>
                        <% quotation.extraCharges.forEach(charge => { %>
                            <tr class="service-row">
                                <td class="text-center"><%= serviceCounter %></td>
                                <td class="service-name"><%= charge.description || charge.name || 'Service Charge' %></td>
                                <td class="text-center">9967</td>
                                <% if (quotation.gstEnabled && (quotation.documentType === 'GST' || quotation.documentType === 'gst')) { %>
                                <td class="text-right amount-cell">₹<%= parseFloat(charge.amount || 0).toFixed(2) %></td>
                                <td class="text-center"><%= charge.gstRate || 0 %>%</td>
                                <% } else { %>
                                <td class="text-right amount-cell">₹<%= parseFloat(charge.amount || 0).toFixed(2) %></td>
                                <% } %>
                            </tr>
                            <% serviceCounter++; %>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
        <% } %>

        <!-- Totals Section -->
        <div class="totals-section">
            <div class="totals-wrapper">
                <!-- Quote Information Card -->
                <div class="payment-card">
                    <div class="card-header">
                        💜 Quote Information
                    </div>
                    
                    <div class="payment-row">
                        <span class="row-label">Quote Status:</span>
                        <span class="row-value">
                            <span class="status-badge status-<%= status.toLowerCase() %>"><%= status.toUpperCase() %></span>
                        </span>
                    </div>
                    
                    <div class="payment-row">
                        <span class="row-label">Quote Date:</span>
                        <span class="row-value"><%= new Date(quotation.quotationDate).toLocaleDateString('en-IN') %></span>
                    </div>
                    
                    <div class="payment-row">
                        <span class="row-label">Valid Until:</span>
                        <span class="row-value"><%= validityDate.toLocaleDateString('en-IN') %></span>
                    </div>
                    
                    <div class="payment-row">
                        <span class="row-label">Validity:</span>
                        <span class="row-value"><%= quotation.validityDays %> days</span>
                    </div>
                    
                    <div style="margin-top: 20px; padding: 15px; background: #f8f9fa; border-radius: 8px; text-align: center;">
                        <p style="margin: 0; font-size: 14px; color: #495057;">
                            <strong>Thank you for your interest!</strong><br>
                            This quotation is computer generated and subject to terms mentioned below.
                        </p>
                    </div>
                </div>
                
                <!-- Totals Card -->
                <div class="totals-card">
                    <div class="card-header">
                        💰 Quote Summary
                    </div>
                    
                    <div class="total-row-new">
                        <span class="row-label">Subtotal:</span>
                        <span class="row-value">₹<%= quotation.subtotal.toFixed(2) %></span>
                    </div>
                    
                    <% if (quotation.discount && quotation.discount > 0) { %>
                        <div class="total-row-new">
                            <span class="row-label">Discount:</span>
                            <span class="row-value">-₹<%= quotation.discount.toFixed(2) %></span>
                        </div>
                    <% } %>
                    
                    <% if (quotation.gstEnabled && (quotation.documentType === 'GST' || quotation.documentType === 'gst') && quotation.cgstAmount > 0) { %>
                        <div class="total-row-new">
                            <span class="row-label">CGST:</span>
                            <span class="row-value">₹<%= quotation.cgstAmount.toFixed(2) %></span>
                        </div>
                        <div class="total-row-new">
                            <span class="row-label">SGST:</span>
                            <span class="row-value">₹<%= quotation.sgstAmount.toFixed(2) %></span>
                        </div>
                        <div class="total-row-new">
                            <span class="row-label">Total Tax:</span>
                            <span class="row-value">₹<%= (quotation.cgstAmount + quotation.sgstAmount).toFixed(2) %></span>
                        </div>
                    <% } %>
                    
                    <% if (quotation.roundOffAmount && Math.abs(quotation.roundOffAmount) > 0) { %>
                        <div class="total-row-new">
                            <span class="row-label">Round Off:</span>
                            <span class="row-value"><%= quotation.roundOffAmount >= 0 ? '+' : '' %>₹<%= quotation.roundOffAmount.toFixed(2) %></span>
                        </div>
                    <% } %>
                    
                    <div class="final-amount">
                        <div class="amount-label">Total Amount</div>
                        <div class="amount-value">₹<%= quotation.finalTotal.toFixed(2) %></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Terms and Validity Section -->
        <div class="footer-info">
            <div class="terms-section">
                <h4>📋 Terms & Conditions</h4>
                <ul>
                    <li>This quotation is valid for <%= quotation.validityDays %> days from date of issue</li>
                    <li>Prices are subject to change without prior notice</li>
                    <li>All rates are inclusive of GST as applicable</li>
                    <li>Payment terms to be mutually agreed upon order confirmation</li>
                    <li>Delivery timeline will be confirmed at time of order placement</li>
                    <li>Subject to local jurisdiction only</li>
                </ul>
            </div>
            
            <div class="validity-section">
                <h4>⏰ Important Notes</h4>
                <p><strong>Quote Validity:</strong> <%= quotation.validityDays %> days from <%= new Date(quotation.quotationDate).toLocaleDateString('en-IN') %></p>
                <p><strong>Valid Until:</strong> <%= validityDate.toLocaleDateString('en-IN') %></p>
                <p><strong>Current Status:</strong> 
                    <span class="status-badge status-<%= status.toLowerCase() %>"><%= status %></span>
                </p>
                <% if (company.bankDetails) { %>
                <p><strong>For Advance Payment:</strong></p>
                <p>A/c Holder's Name: <%= company.bankDetails.accountName || company.name %></p>
                <p>Bank Name: <%= company.bankDetails.bankName || 'Available on Request' %></p>
                <p>A/c No: <%= company.bankDetails.accountNumber || 'XXXX-XXXX' %></p>
                <p>Branch & IFSC Code: <%= company.bankDetails.branchDetails || 'XXXX0000XXX' %></p>
                <% } %>
            </div>
        </div>
    </div>

    <!-- Actions -->
    <div class="actions">
        <div class="action-buttons">
            <a href="/quotations" class="btn btn-secondary">
                <span class="icon">←</span>
                <span>Back to Quotations</span>
            </a>
            
            <button onclick="printQuote()" class="btn btn-primary">
                <span class="icon">🖨️</span>
                <span>Print Quote</span>
            </button>
            
            <a href="/quotations/<%= quotation.id %>/print" target="_blank" class="btn btn-print-compact">
                <span class="icon">📄</span>
                <span>Compact Print</span>
            </a>
            
            <% if (status !== 'Converted' && status !== 'Expired') { %>
            <button onclick="convertToBill('<%= quotation.id %>')" class="btn btn-convert">
                <span class="icon">🔄</span>
                <span>Convert to Bill</span>
            </button>
            <% } %>
            
            <a href="/quotations/new" class="btn btn-success">
                <span class="icon">➕</span>
                <span>New Quote</span>
            </a>
        </div>
    </div>

    <!-- JavaScript -->
    <script>
        // Auto-hide alerts
        setTimeout(() => {
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(alert => {
                alert.style.opacity = '0';
                setTimeout(() => alert.remove(), 300);
            });
        }, 5000);

        // Enhanced Print Function
        function printQuote() {
            // Add print-specific class to body
            document.body.classList.add('printing');
            
            // Optimize for print
            const originalTitle = document.title;
            document.title = 'Quotation-<%= quotation.quotationNumber %>-<%= new Date().toISOString().split("T")[0] %>';
            
            // Add print date for reference
            const printDate = new Date().toLocaleDateString('en-IN', {
                year: 'numeric',
                month: 'short',
                day: 'numeric',
                hour: '2-digit',
                minute: '2-digit'
            });
            
            // Create print footer
            const printFooter = document.createElement('div');
            printFooter.className = 'print-only';
            printFooter.style.cssText = `
                position: fixed;
                bottom: 0;
                left: 0;
                right: 0;
                text-align: center;
                font-size: 8px;
                color: #666;
                background: white;
                padding: 5px;
                border-top: 1px solid #ddd;
                z-index: 1000;
            `;
            printFooter.innerHTML = `Printed on: ${printDate} | Quotation: <%= quotation.quotationNumber %>`;
            
            document.body.appendChild(printFooter);
            
            // Trigger print
            window.print();
            
            // Cleanup after print
            setTimeout(() => {
                document.body.classList.remove('printing');
                document.title = originalTitle;
                if (printFooter.parentNode) {
                    printFooter.parentNode.removeChild(printFooter);
                }
            }, 1000);
        }

        function convertToBill(quotationId) {
            if (confirm('🔄 Are you sure you want to convert this quotation to a bill?\\n\\n' +
                       '✅ This will create a new bill\\n' +
                       '📦 Stock levels will be updated\\n' +
                       '👤 Customer data will be saved\\n' +
                       '💜 Quotation will be marked as converted\\n\\n' +
                       'This action cannot be undone!')) {
                
                // Show loading state
                const btn = event.target.closest('button');
                const originalText = btn.innerHTML;
                btn.innerHTML = '<span>⏳</span> Converting...';
                btn.disabled = true;
                
                fetch(`/quotations/${quotationId}/convert`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    }
                })
                .then(response => {
                    if (response.redirected) {
                        window.location.href = response.url;
                    } else {
                        return response.text();
                    }
                })
                .then(data => {
                    if (data) {
                        // If we get here, there was an error
                        console.error('Error response:', data);
                        alert('❌ Error converting quotation to bill. Please try again.');
                        btn.innerHTML = originalText;
                        btn.disabled = false;
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('❌ Error converting quotation to bill. Please try again.');
                    btn.innerHTML = originalText;
                    btn.disabled = false;
                });
            }
        }
    </script>
    </main>

    <%- include('../partials/scripts') %>
</body>
</html>