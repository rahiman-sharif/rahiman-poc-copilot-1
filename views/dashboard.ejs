<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- Header -->
    <header class="main-header">
        <div class="header-content">
            <div class="logo-section">
                <a href="/dashboard" style="text-decoration: none; color: inherit;">
                    <h1>🏢 <%= companyName %></h1>
                    <p>Billing Management System</p>
                </a>
            </div>
            
            <div class="user-section">
                <div class="user-info">
                    <span class="user-name">Welcome, <%= user.name %></span>
                    <span class="user-role">(<%= user.role %>)</span>
                </div>
                <form action="/logout" method="POST" style="display: inline;">
                    <button type="submit" class="logout-btn">Logout</button>
                </form>
            </div>
        </div>
    </header>
    <!-- Main Dashboard -->
    <main class="dashboard-main">
        <div class="dashboard-container">
            <!-- Welcome Banner -->
            <div class="welcome-banner">
                <h2>Welcome back, <%= user.name %>!</h2>
                <p>Your business overview and quick actions</p>
            </div>

            <!-- Business Overview -->
            <section class="overview-section">
                <h3 class="section-title">📊 Business Overview</h3>
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-header">
                            <span class="stat-icon">📋</span>
                            <h4>Today's Bills</h4>
                        </div>
                        <div class="stat-value"><%= stats.billsToday %></div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-header">
                            <span class="stat-icon">💰</span>
                            <h4>Today's Revenue</h4>
                        </div>
                        <div class="stat-value">₹<%= stats.revenueToday.toLocaleString() %></div>
                    </div>  
                    
                    <div class="stat-card">
                        <div class="stat-header">
                            <span class="stat-icon">📦</span>
                            <h4>Inventory Items</h4>
                        </div>
                        <div class="stat-value"><%= stats.itemsCount %></div>
                        <% if (stats.lowStockCount > 0) { %>
                            <div class="stat-alert">⚠️ <%= stats.lowStockCount %> low stock</div>
                        <% } %>
                    </div>
                    
                    <!-- License Info Widget -->
                    <div class="stat-card license-card" id="licenseWidget">
                        <div class="stat-header">
                            <span class="stat-icon">🔐</span>
                            <h4>License Status</h4>
                        </div>
                        <div class="license-info">
                            <div class="loading-spinner">Loading...</div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Primary Actions -->
            <section class="actions-section">
                <h3 class="section-title">⚡ Primary Actions</h3>
                <div class="primary-actions">
                    <a href="/bills/new" class="primary-btn create-bill">
                        <span class="btn-icon">📝</span>
                        <span class="btn-text">Create New Bill</span>
                    </a>
                    
                    <a href="/quotations/new" class="primary-btn create-quote">
                        <span class="btn-icon">💜</span>
                        <span class="btn-text">Create Quotation</span>
                    </a>
                </div>
            </section>

            <!-- Management Grid -->
            <section class="management-section">
                <h3 class="section-title">🏢 Business Management</h3>
                <div class="management-grid">
                    <!-- Sales & Orders -->
                    <div class="management-group">
                        <h4 class="group-title">Sales & Orders</h4>
                        <div class="management-cards">
                            <a href="/bills" class="mgmt-card">
                                <span class="mgmt-icon">📋</span>
                                <div class="mgmt-info">
                                    <h5>Bills</h5>
                                    <p>View & manage invoices</p>
                                </div>
                            </a>
                            
                            <a href="/quotations" class="mgmt-card">
                                <span class="mgmt-icon">💜</span>
                                <div class="mgmt-info">
                                    <h5>Quotations</h5>
                                    <p>Manage price quotes</p>
                                </div>
                            </a>

                            <a href="/whatsapp" class="mgmt-card">
                                <span class="mgmt-icon">💬</span>
                                <div class="mgmt-info">
                                    <h5>Send Bills via WhatsApp</h5>
                                    <p>Share bill information</p>
                                </div>
                            </a>
                        </div>
                    </div>

                    <!-- Inventory -->
                    <div class="management-group">
                        <h4 class="group-title">Inventory</h4>
                        <div class="management-cards">
                            <a href="/items" class="mgmt-card">
                                <span class="mgmt-icon">📦</span>
                                <div class="mgmt-info">
                                    <h5>Items</h5>
                                    <p>Product catalog</p>
                                </div>
                            </a>
                            
                            <a href="/stock" class="mgmt-card">
                                <span class="mgmt-icon">📊</span>
                                <div class="mgmt-info">
                                    <h5>Stock</h5>
                                    <p>Inventory tracking</p>
                                </div>
                            </a>
                        </div>
                    </div>

                    <!-- Customer Relations -->
                    <div class="management-group">
                        <h4 class="group-title">Customer Relations</h4>
                        <div class="management-cards">
                            <a href="/customers" class="mgmt-card">
                                <span class="mgmt-icon">👥</span>
                                <div class="mgmt-info">
                                    <h5>Customers</h5>
                                    <p>Customer directory</p>
                                </div>
                            </a>
                            
                            <a href="/reports" class="mgmt-card">
                                <span class="mgmt-icon">📈</span>
                                <div class="mgmt-info">
                                    <h5>Reports</h5>
                                    <p>Sales analytics</p>
                                </div>
                            </a>
                        </div>
                    </div>

                    <% if (user.role === 'admin' || user.role === 'super') { %>
                    <!-- System Administration -->
                    <div class="management-group admin-only">
                        <h4 class="group-title">System Administration</h4>
                        <div class="management-cards">
                            <a href="/users" class="mgmt-card">
                                <span class="mgmt-icon">👤</span>
                                <div class="mgmt-info">
                                    <h5>Users</h5>
                                    <p>Manage system users</p>
                                </div>
                            </a>                            
                           
                            <% if (user && user.username === 'superuser' && user.role === 'super') { %>
                            <!-- Super Admin License Generator -->
                            <a href="/license/generate" class="mgmt-card" style="border: 2px solid #ff6b6b;">
                                <span class="mgmt-icon">🎫</span>
                                <div class="mgmt-info">
                                    <h5>License Generator</h5>
                                    <p>Generate license certificates</p>
                                </div>
                                <div style="position: absolute; top: 5px; right: 5px; background: linear-gradient(45deg, #ff6b6b, #ee5a24); color: white; padding: 2px 8px; border-radius: 10px; font-size: 0.7em; font-weight: bold;">
                                    SUPER ADMIN
                                </div>
                            </a>
                            <% } %>
                            
                            <!-- License Upload (All Users) -->
                            <a href="/license/upload" class="mgmt-card">
                                <span class="mgmt-icon">📄</span>
                                <div class="mgmt-info">
                                    <h5>Upload License</h5>
                                    <p>Update application license</p>
                                </div>
                            </a>
                            
                            <!-- Update Company Data (All Admin Users) -->
                            <a href="/settings/company-upload" class="mgmt-card">
                                <span class="mgmt-icon">📤</span>
                                <div class="mgmt-info">
                                    <h5>Update Company Data</h5>
                                    <p>Upload encrypted company file</p>
                                </div>
                            </a>
                           
                            <% if (user && user.role === 'super') { %>
                            <a href="/settings/company" class="mgmt-card">
                                <span class="mgmt-icon">🏢</span>
                                <div class="mgmt-info">
                                    <h5>Company Setup</h5>
                                    <p>Configure company details</p>
                                </div>
                            </a>
                            
                            <a href="/settings/route-control" class="mgmt-card route-control-card">
                                <span class="mgmt-icon">🛣️</span>
                                <div class="mgmt-info">
                                    <h5>Route Control Center</h5>
                                    <p>Manage client demo features</p>
                                </div>
                            </a>
                            <% } %>
                            
                            <a href="/data" class="mgmt-card">
                                <span class="mgmt-icon">💾</span>
                                <div class="mgmt-info">
                                    <h5>Data</h5>
                                    <p>Backup & restore</p>
                                </div>
                            </a>
                        </div>
                    </div>
                    <% } %>
                </div>
            </section>
        </div>
    </main>

    <footer class="main-footer">
        <p>&copy; 2025 <%= companyName %> Billing System. Built for offline use.</p>
    </footer>

    <!-- License Widget Styles -->
    <style>
        .license-card {
            position: relative;
        }
        .license-info {
            min-height: 60px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .license-status {
            font-size: 1.2em;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .license-status.valid {
            color: #28a745;
        }
        .license-status.warning {
            color: #ffc107;
        }
        .license-status.expired {
            color: #dc3545;
        }
        .license-details {
            font-size: 0.9em;
            color: #666;
        }
        .license-warning {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            padding: 8px;
            border-radius: 4px;
            margin-top: 5px;
            font-size: 0.8em;
            color: #856404;
        }
        .license-expired {
            background: #f8d7da;
            border: 1px solid #f5c6cb;
            padding: 8px;
            border-radius: 4px;
            margin-top: 5px;
            font-size: 0.8em;
            color: #721c24;
        }
        .loading-spinner {
            text-align: center;
            color: #666;
            font-size: 0.9em;
        }
        .license-actions {
            margin-top: 8px;
        }
        .license-btn {
            background: #007bff;
            color: white;
            padding: 4px 8px;
            border: none;
            border-radius: 3px;
            font-size: 0.8em;
            text-decoration: none;
            display: inline-block;
        }
        .license-btn:hover {
            background: #0056b3;
            color: white;
            text-decoration: none;
        }
        .license-btn.upload {
            background: #28a745;
        }
        .license-btn.upload:hover {
            background: #1e7e34;
        }
    </style>

    <!-- License Widget Script -->
    <script>
        // Load license info on page load
        document.addEventListener('DOMContentLoaded', function() {
            loadLicenseInfo();
        });

        async function loadLicenseInfo() {
            const widget = document.getElementById('licenseWidget');
            const infoContainer = widget.querySelector('.license-info');
            
            try {
                const response = await fetch('/license/info');
                const licenseInfo = await response.json();
                
                if (response.ok) {
                    displayLicenseInfo(licenseInfo, infoContainer);
                } else {
                    infoContainer.innerHTML = `
                        <div class="license-status expired">License Error</div>
                        <div class="license-details">Failed to load license information</div>
                    `;
                }
            } catch (error) {
                console.error('Error loading license info:', error);
                infoContainer.innerHTML = `
                    <div class="license-status expired">Connection Error</div>
                    <div class="license-details">Unable to check license status</div>
                `;
            }
        }

        function displayLicenseInfo(info, container) {
            if (info.status === 'valid') {
                let statusClass = 'valid';
                let statusText = 'Active';
                let warningHtml = '';
                
                if (info.isExpiringSoon) {
                    statusClass = 'warning';
                    statusText = 'Expiring Soon';
                    warningHtml = `
                        <div class="license-warning">
                            ⚠️ Expires in ${info.daysRemaining} days
                        </div>
                    `;
                }
                
                container.innerHTML = `
                    <div class="license-status ${statusClass}">${statusText}</div>
                    <div class="license-details">
                        Expires: ${info.expiryDate}<br>
                        ${info.daysRemaining} days remaining
                    </div>
                    ${warningHtml}
                    <div class="license-actions">
                        <a href="/license/upload" class="license-btn upload">Update License</a>
                    </div>
                `;
            } else {
                container.innerHTML = `
                    <div class="license-status expired">Invalid</div>
                    <div class="license-details">${info.message || 'License issue detected'}</div>
                    <div class="license-expired">
                        🚨 Please upload a valid license
                    </div>
                    <div class="license-actions">
                        <a href="/license/upload" class="license-btn upload">Upload License</a>
                    </div>
                `;
            }
        }

        // Refresh license info every 5 minutes
        setInterval(loadLicenseInfo, 5 * 60 * 1000);
    </script>
</body>
</html>
