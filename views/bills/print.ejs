<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bill #<%= bill.billNumber %> - <%= bill.customerName %></title>
    <style>
        /* Print-optimized styling */
        @media print {
            body { 
                margin: 0; 
                font-size: 5px;
                line-height: 1.0;
            }
            .no-print { display: none !important; }
            .print-break { page-break-after: always; }
            .invoice-container {
                border: none;
                box-shadow: none;
                margin: 0;
                padding: 0;
            }
            
            .company-name {
                font-size: 10px !important;
                font-weight: bold !important;
                margin-bottom: 1px !important;
            }
            
            .company-details {
                font-size: 5px !important;
                line-height: 1.0 !important;
                margin-bottom: 2px !important;
            }
            
            .invoice-title {
                font-size: 8px !important;
                padding: 2px !important;
                margin: 2px 0 !important;
            }
            
            .section-title {
                font-size: 5px !important;
                margin-bottom: 1px !important;
            }
            
            .info-label, .info-value {
                font-size: 5px !important;
                margin-bottom: 0 !important;
            }
            
            .items-table th {
                padding: 1px 1px !important;
                font-size: 5px !important;
                line-height: 1.0 !important;
            }
            
            .items-table td {
                padding: 1px 1px !important;
                font-size: 5px !important;
                line-height: 1.0 !important;
            }
            
            .item-name {
                font-size: 5px !important;
                line-height: 1.0 !important;
            }
            
            .item-details {
                font-size: 4px !important;
                line-height: 1.0 !important;
            }
            
            .amount-column {
                font-size: 5px !important;
            }
            
            .charges-table th, .charges-table td {
                padding: 1px 1px !important;
                font-size: 5px !important;
            }
            
            .bill-details {
                padding: 2px 5px !important;
                gap: 5px !important;
            }
            
            .items-section {
                padding: 2px 5px !important;
            }
            
            .totals-section {
                padding: 2px 5px !important;
            }
            
            .invoice-header {
                padding: 2px 5px !important;
            }
            
            .bill-info, .customer-info {
                padding: 2px !important;
                margin-bottom: 2px !important;
            }
            
            .info-row {
                margin-bottom: 0 !important;
                padding: 0 !important;
            }
            
            .totals-table {
                font-size: 5px !important;
            }
            
            .totals-table td {
                padding: 1px 2px !important;
            }
            
            .totals-table .final-total {
                font-size: 6px !important;
            }
        }
        
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 5px;
            background: white;
            color: #333;
            font-size: 6px;
            line-height: 1.1;
        }
        
        .invoice-container {
            max-width: 100%;
            margin: 0 auto;
            background: white;
            border: 1px solid #ddd;
        }
        
        .invoice-header {
            background: #f8f9fa;
            padding: 4px 8px;
            border-bottom: 2px solid #007bff;
            text-align: center;
        }
        
        .company-name {
            font-size: 12px;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 1px;
        }
        
        .company-details {
            font-size: 6px;
            color: #666;
            line-height: 1.1;
        }
        
        .invoice-title {
            text-align: center;
            font-size: 10px;
            font-weight: bold;
            color: white;
            margin: 4px 0;
            text-transform: uppercase;
            letter-spacing: 1px;
            background: #007bff;
            padding: 3px;
        }
        
        .bill-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 8px;
            padding: 4px 8px;
            border-bottom: 1px solid #eee;
        }
        
        .bill-info, .customer-info {
            background: #f8f9fa;
            padding: 4px;
            border-radius: 3px;
        }
        
        .section-title {
            font-weight: bold;
            color: #007bff;
            margin-bottom: 2px;
            font-size: 6px;
        }
        
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1px;
            padding: 0;
        }
        
        .info-label {
            font-weight: 500;
            color: #555;
            font-size: 5px;
        }
        
        .info-value {
            font-weight: bold;
            color: #333;
            font-size: 5px;
        }
        
        .items-section {
            padding: 4px 8px;
        }
        
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 4px;
            font-size: 5px;
        }
        
        .items-table th {
            background: #007bff;
            color: white;
            padding: 2px 1px;
            text-align: left;
            font-weight: bold;
            border: 1px solid #007bff;
            font-size: 5px;
        }
        
        .items-table td {
            padding: 1px 1px;
            border: 1px solid #ddd;
            vertical-align: top;
            font-size: 5px;
        }
        
        .items-table tbody tr:nth-child(even) {
            background: #f8f9fa;
        }
        
        .item-name {
            font-weight: bold;
            color: #333;
            font-size: 5px;
        }
        
        .item-details {
            font-size: 4px;
            color: #666;
            margin-top: 0;
        }
        
        .amount-column {
            text-align: right;
            font-weight: bold;
            font-size: 5px;
        }
        
        .charges-section {
            margin-top: 4px;
        }
        
        .charges-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 4px;
            font-size: 5px;
        }
        
        .charges-table th {
            background: #28a745;
            color: white;
            padding: 2px 1px;
            text-align: left;
            border: 1px solid #28a745;
            font-size: 5px;
        }
        
        .charges-table td {
            padding: 1px;
            border: 1px solid #ddd;
            font-size: 5px;
        }
        
        .totals-section {
            background: #f8f9fa;
            padding: 4px 8px;
            border-top: 2px solid #007bff;
        }
        
        .totals-grid {
            display: grid;
            grid-template-columns: 1fr auto;
            gap: 6px;
        }
        
        .payment-info {
            padding: 2px;
            background: white;
            border: 1px solid #ddd;
            border-radius: 3px;
            font-size: 5px;
        }
        
        .totals-table {
            min-width: 120px;
            font-size: 5px;
        }
        
        .totals-table tr {
            border-bottom: 1px solid #eee;
        }
        
        .totals-table td {
            padding: 1px 2px;
            text-align: right;
        }
        
        .totals-table .label {
            text-align: left;
            font-weight: 500;
            color: #555;
            font-size: 5px;
        }
        
        .totals-table .final-total {
            background: #007bff;
            color: white;
            font-weight: bold;
            font-size: 6px;
        }
        
        .print-controls {
            text-align: center;
            padding: 15px;
            border-top: 1px solid #eee;
        }
        
        .btn {
            display: inline-block;
            padding: 8px 16px;
            margin: 0 5px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            font-weight: bold;
            transition: all 0.3s;
            font-size: 12px;
        }
        
        .btn-primary {
            background: #007bff;
            color: white;
        }
        
        .btn-secondary {
            background: #6c757d;
            color: white;
        }
        
        .btn:hover {
            opacity: 0.9;
            transform: translateY(-1px);
        }
        
        .payment-method-badge {
            display: inline-block;
            background: #28a745;
            color: white;
            padding: 2px 6px;
            border-radius: 2px;
            font-size: 8px;
            font-weight: bold;
            text-transform: uppercase;
        }
        
        .upi-badge { background: #ff6b35; }
        .bank-badge { background: #007bff; }
        .cheque-badge { background: #6f42c1; }
        .card-badge { background: #fd7e14; }
        .credit-badge { background: #dc3545; }
        
        /* Compact spacing for print */
        h3, h4 {
            margin: 8px 0 6px 0;
            font-size: 11px;
        }
        
        /* More items per page */
        @media print {
            .items-table th,
            .items-table td {
                padding: 2px 3px;
                font-size: 8px;
            }
            
            .company-name {
                font-size: 16px;
            }
            
            .invoice-title {
                font-size: 14px;
                padding: 4px;
                margin: 6px 0;
            }
            
            .bill-details {
                padding: 8px 12px;
                gap: 10px;
            }
            
            .items-section {
                padding: 8px 12px;
            }
            
            .totals-section {
                padding: 8px 12px;
            }
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <!-- Header -->
        <div class="invoice-header">
            <div class="company-name">🏢 <%= company.name || companyName %></div>
            <div class="company-details">
                <% if (company.address) { %>
                    <%= company.address.line1 %>, <%= company.address.line2 %><br>
                    <%= company.address.city %>, <%= company.address.state %> - <%= company.address.pincode %><br>
                <% } %>
                <% if (company.contact) { %>
                    Cell: <%= company.contact.phone1 %>, <%= company.contact.phone2 %><br>
                <% } %>
                <% if (company.gst && company.gst.gstin) { %>
                    GST No: <%= company.gst.gstin %>
                <% } %>
            </div>
        </div>

        <!-- Invoice Title -->
        <div class="invoice-title">Tax Invoice</div>

        <!-- Bill Details -->
        <div class="bill-details">
            <div class="bill-info">
                <div class="section-title">📄 Bill Information</div>
                <div class="info-row">
                    <span class="info-label">Bill Number:</span>
                    <span class="info-value">#<%= bill.billNumber %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Date:</span>
                    <span class="info-value"><%= new Date(bill.billDate).toLocaleDateString() %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Time:</span>
                    <span class="info-value"><%= new Date(bill.billDate).toLocaleTimeString() %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Payment Method:</span>
                    <span class="info-value">
                        <span class="payment-method-badge <%= (bill.paymentMethod || 'Cash').toLowerCase() %>-badge">
                            <%= bill.paymentMethod || 'Cash' %>
                        </span>
                    </span>
                </div>
                <% if (bill.paymentDetails && Object.keys(bill.paymentDetails).length > 1) { %>
                    <div style="margin-top: 6px; padding-top: 6px; border-top: 1px solid #eee;">
                        <div style="font-size: 9px; color: #666; margin-bottom: 3px;">Payment Details:</div>
                        <% if (bill.paymentDetails.transactionId) { %>
                            <div style="font-size: 8px;">Transaction ID: <%= bill.paymentDetails.transactionId %></div>
                        <% } %>
                        <% if (bill.paymentDetails.referenceNumber) { %>
                            <div style="font-size: 8px;">Reference: <%= bill.paymentDetails.referenceNumber %></div>
                        <% } %>
                        <% if (bill.paymentDetails.chequeNumber) { %>
                            <div style="font-size: 8px;">Cheque #: <%= bill.paymentDetails.chequeNumber %></div>
                        <% } %>
                        <% if (bill.paymentDetails.last4Digits) { %>
                            <div style="font-size: 8px;">Card: ****<%= bill.paymentDetails.last4Digits %></div>
                        <% } %>
                        <% if (bill.paymentDetails.bankName) { %>
                            <div style="font-size: 8px;">Bank: <%= bill.paymentDetails.bankName %></div>
                        <% } %>
                    </div>
                <% } %>
            </div>
            
            <div class="customer-info">
                <div class="section-title">👤 Customer Information</div>
                <div class="info-row">
                    <span class="info-label">Name:</span>
                    <span class="info-value"><%= bill.customerName %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Phone:</span>
                    <span class="info-value"><%= bill.customerPhone %></span>
                </div>
                <% if (bill.customerGstin) { %>
                    <div class="info-row">
                        <span class="info-label">GST Number:</span>
                        <span class="info-value"><%= bill.customerGstin %></span>
                    </div>
                <% } %>
            </div>
        </div>

        <!-- Items Section -->
        <div class="items-section">
            <h3 style="color: #007bff; margin-bottom: 8px; font-size: 11px;">📦 Items & Services</h3>
            <table class="items-table">
                <thead>
                    <tr>
                        <th style="width: 4%;">S.No</th>
                        <th style="width: 40%;">Description</th>
                        <th style="width: 8%;">HSN</th>
                        <th style="width: 8%;">Qty</th>
                        <th style="width: 12%;">Rate</th>
                        <th style="width: 8%;">GST %</th>
                        <th style="width: 15%;">Amount</th>
                    </tr>
                </thead>
                <tbody>
                    <% bill.items.forEach((item, index) => { %>
                        <tr>
                            <td style="text-align: center;"><%= index + 1 %></td>
                            <td>
                                <div class="item-name"><%= item.itemName %></div>
                                <% if (!item.isServiceItem) { %>
                                    <div class="item-details">Physical Item</div>
                                <% } else { %>
                                    <div class="item-details">Service Item</div>
                                <% } %>
                            </td>
                            <td style="text-align: center;"><%= item.hsnCode %></td>
                            <td style="text-align: center;"><%= item.quantity %></td>
                            <td class="amount-column">₹<%= item.rate.toLocaleString() %></td>
                            <td style="text-align: center;"><%= item.gstRate %>%</td>
                            <td class="amount-column">₹<%= item.amount.toLocaleString() %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>

            <!-- Extra Charges -->
            <% if (bill.extraCharges && bill.extraCharges.length > 0) { %>
                <div class="charges-section">
                    <h4 style="color: #28a745; margin-bottom: 6px; font-size: 10px;">💰 Additional Charges</h4>
                    <table class="charges-table">
                        <thead>
                            <tr>
                                <th>Description</th>
                                <th style="width: 15%;">GST %</th>
                                <th style="width: 20%;">Amount</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% bill.extraCharges.forEach(charge => { %>
                                <tr>
                                    <td><%= charge.description %></td>
                                    <td style="text-align: center;"><%= charge.gstRate %>%</td>
                                    <td class="amount-column">₹<%= charge.amount.toLocaleString() %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } %>
        </div>

        <!-- Totals Section -->
        <div class="totals-section">
            <div class="totals-grid">
                <div class="payment-info">
                    <div class="section-title">💳 Payment Information</div>
                    <div><strong>Method:</strong> <%= bill.paymentMethod || 'Cash' %></div>
                    <% if (bill.paymentDetails && bill.paymentDetails.transactionId) { %>
                        <div><strong>Transaction ID:</strong> <%= bill.paymentDetails.transactionId %></div>
                    <% } %>
                    <% if (bill.paymentDetails && bill.paymentDetails.dueDate) { %>
                        <div><strong>Due Date:</strong> <%= new Date(bill.paymentDetails.dueDate).toLocaleDateString() %></div>
                    <% } %>
                    <% if (bill.paymentDetails && bill.paymentDetails.terms) { %>
                        <div><strong>Terms:</strong> <%= bill.paymentDetails.terms %></div>
                    <% } %>
                </div>
                
                <table class="totals-table">
                    <tr>
                        <td class="label">Subtotal:</td>
                        <td>₹<%= bill.subtotal.toLocaleString() %></td>
                    </tr>
                    <% if (bill.cgstAmount > 0) { %>
                        <tr>
                            <td class="label">CGST:</td>
                            <td>₹<%= bill.cgstAmount.toLocaleString() %></td>
                        </tr>
                        <tr>
                            <td class="label">SGST:</td>
                            <td>₹<%= bill.sgstAmount.toLocaleString() %></td>
                        </tr>
                    <% } %>
                    <tr>
                        <td class="label">Total GST:</td>
                        <td>₹<%= bill.totalGst.toLocaleString() %></td>
                    </tr>
                    <tr class="final-total">
                        <td class="label">Grand Total:</td>
                        <td>₹<%= bill.finalTotal.toLocaleString() %></td>
                    </tr>
                </table>
            </div>
        </div>

        <!-- Tax Amount in Words -->
        <div style="padding: 4px 8px; border-top: 1px solid #eee; font-size: 6px;">
            <div style="margin-bottom: 3px;">
                <strong>Tax Amount (in words):</strong> <%= bill.totalGstInWords || 'Indian Rupees Sixteen Thousand Nine Hundred Fifty Three and Fifty Six paise Only' %>
            </div>
        </div>

        <!-- Company Bank Details and Declaration -->
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 8px; padding: 4px 8px; border-top: 1px solid #eee; font-size: 6px;">
            <div>
                <div style="font-weight: bold; color: #007bff; margin-bottom: 3px;">Company's Bank Details</div>
                <div><strong>A/c Holder's Name:</strong> <%= company && company.bankDetails ? company.bankDetails.accountHolderName : 'MP STEELS CORPORATION' %></div>
                <div><strong>Bank Name:</strong> <%= company && company.bankDetails ? company.bankDetails.bankName : 'TAMILNAD MERCANTILE BANK LTD.' %></div>
                <div><strong>A/c No:</strong> <%= company && company.bankDetails ? company.bankDetails.accountNumber : '11870150950104' %></div>
                <div><strong>Branch:</strong> <%= company && company.bankDetails ? company.bankDetails.branchDetails : 'Chinthamani, Madurai & TMBL0000118' %></div>
                <div style="margin-top: 5px;"><strong>Company's PAN:</strong> <%= company && company.gst ? company.gst.panNo || 'AFQPD9332R' : 'AFQPD9332R' %></div>
            </div>
            
            <div>
                <div style="font-weight: bold; color: #007bff; margin-bottom: 3px;">Declaration</div>
                <div style="line-height: 1.2; margin-bottom: 8px;">
                    We declare that this invoice shows the actual price of the goods described and that all particulars are true and correct.
                </div>
                <div style="text-align: right; margin-top: 15px; border-top: 1px solid #ccc; padding-top: 5px;">
                    <strong>Authorized Signature</strong>
                </div>
            </div>
        </div>

        <!-- Print Controls -->
        <div class="print-controls no-print">
            <button onclick="window.print()" class="btn btn-primary">🖨️ Print Bill</button>
            <a href="/bills/<%= bill.id %>" class="btn btn-secondary">👁️ View Bill</a>
            <a href="/bills" class="btn btn-secondary">📋 All Bills</a>
        </div>
    </div>

    <script>
        // Auto-print when page loads (optional)
        // window.onload = function() { window.print(); };
        
        // Print functionality
        function printBill() {
            window.print();
        }
        
        // Optional: Print when user presses Ctrl+P
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && e.key === 'p') {
                e.preventDefault();
                window.print();
            }
        });
    </script>
</body>
</html>
